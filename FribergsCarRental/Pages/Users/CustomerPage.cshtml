@page
@using FribergsCarRental.Data.Models;
@model FribergsCarRental.Pages.Users.CustomerPageModel

@{
    ViewData["Title"] = "CustomerPage";
}

<h1>Välkommen @Model.UserFirstName! </h1>
<a asp-page="/Users/CustomerCarIndex">Välj och boka en bil</a>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].Car)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].TheUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Booking)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Car.Brand)
                    @Html.DisplayFor(modelItem => item.Car.Model)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.StartDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TheUser.FirstName)
                    @Html.DisplayFor(modelItem => item.TheUser.LastName)
                </td>
            </tr>
        }
    </tbody>
</table>









@* Lista över bokningar

*@

@*  <table class="table">
        <thead>
            <tr>
            <th>
                @Html.DisplayNameFor(model => model.Car[0].Model)
            </th>
                <th>
                    @Html.DisplayNameFor(model => model.Car[0].Brand)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Car[0].Model)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Car[0].PictureURL)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Car)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Brand)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Model)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PictureURL)
                    </td>
                    <td>
                        <a asp-page="/Bookings/CreateBooking" asp-route-id="@item.CarId">Boka</a>
                    <a asp-page="/Bookings/BookingIndex" asp-page-handler="OnGetCreateBooking" asp-route-id="@item.CarId">Boka</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
 *@
@* <div>
<a asp-page="/Cars/CarIndex" asp-page-handler="OnGetCars">Bilar</a> |
 <a asp-page="/Bookings/BookingIndex" asp-page-handler="OnGetBookings">Bokningar</a> |
 <a asp-page="/Bookings/BookingIndex">Bokningar</a>
</div> *@