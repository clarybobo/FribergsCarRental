@page
@model FribergsCarRental.Pages.Cars.CarIndexModel

@{
    ViewData["Title"] = "CarIndex";
}
<h1>CarIndex</h1>

    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Car[0].Brand)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Car[0].Model)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Car[0].PictureURL)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Car)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Brand)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Model)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PictureURL)
                    </td>                 
                    <td>
                        <a asp-page="EditCar" asp-route-id="@item.CarId">Edit</a> |
                        <a asp-page="Details" asp-route-id="@item.CarId">Details</a> |
                        <a asp-page="DeleteCar" asp-route-id="@item.CarId">Delete</a> |
                    </td>         

                </tr>
            }
        </tbody>
    </table>
    <a asp-page="/Users/AdminPage">Tillbaka</a>
}
@* 
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Car[0].Brand)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Car[0].Model)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Car[0].PictureURL)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Car)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Brand)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Model)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PictureURL)
                    </td>
                    <td>
                        <a asp-page="Booking" asp-route-id="@item.CarId">Boka</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <a asp-page="/Users/CustomerPage">Tillbaka</a>
}
} *@

@* 
<h1>CarIndex</h1>
<p>
    @if (Model.TheUser.Any(u => u.IsAdmin))
    {
        <a asp-page="CreateCar">Create New</a>
    }
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Car[0].Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Car[0].Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Car[0].PictureURL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Car)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Brand)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Model)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PictureURL)
                </td>
                @if (Model.TheUser.Any(u => u.IsAdmin))
                {
                    <td>
                        <a asp-page="EditCar" asp-route-id="@item.CarId">Edit</a> |
                    <a asp-page="Details" asp-route-id="@item.CarId">Details</a> |
                    <a asp-page="DeleteCar" asp-route-id="@item.CarId">Delete</a> |
                    </td>
                }
                else
                {
                    <td>
                        <a asp-page="Booking" asp-route-id="@item.CarId">Boka</a>
                    </td>
                }               

            </tr>
        }
    </tbody>     
</table>
<a asp-page="/Users/AdminPage">Tillbaka</a>  *@